<%= link_to 'Ask Question', (new_node_path + '?type=Question'), :class => 'button success' %>

<h1>Questions</h1>

<%= render 'empty', element_name: 'question' if @questions.blank? -%>

<ul>
	<% @questions.reverse.each do |question| %>
		
		<li>
			<div class='question'>
				<div class='summary'>
					<%= link_to question.summary, node_path(question) %>
				</div>
			</div>
		</li>
		
	<% end -%>
</ul>
