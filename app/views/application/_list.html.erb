<%= open_tag(list_type).html_safe %>
	<% collection.each do |element| -%>
		<li>
			<%= render 'node', node: element, id_or_class: 'class' -%>
		</li>
	<% end -%>
<%= close_tag(list_type).html_safe %>