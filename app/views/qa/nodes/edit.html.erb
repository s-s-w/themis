<p><%= link_to 'cancel', node_path(@node) %></p>

<br>
<br>
<br>

<%= render 'tree', node: @node.root, selected: @node -%>

<h2>
	Edit <%= type_for(@node).titleize %>
</h2>

<%= render 'form', node: @node -%>
