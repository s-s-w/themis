<%= link_to 'Ask Question', new_node_path, id: 'question_button', style: 'width:auto; padding:20px;' %>

<h1>Questions</h1>

<%= render 'empty', element_name: 'question' if @questions.blank? -%>

<%= render 'list', collection: @questions, list_type: :ul, show_body: true if @questions.any? -%>
